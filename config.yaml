Service:
  name: "dog"
  port: ":9001"
Mysqk:
  host: ${DB_HOST}
  port: ${DB_PORT}
  user: ${DB_USER}
  password: ${DB_PASSWORD}
  dbname: ${DB_NAME}
Redis:
  host: ${REDIS_ADDR}
  port: ${REDIS_PORT}
  password: ""
  db: 0
  pool_size: 100
Ethclient:
  url: "https://sepolia.infura.io/xxxxxxxxx"